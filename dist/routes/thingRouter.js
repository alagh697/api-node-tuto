"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = __importDefault(require("express"));
const thingModel = __importStar(require("../models/thing"));
const multer_1 = __importDefault(require("multer"));
const thingRouter = express_1.default.Router();
exports.default = thingRouter;
// Set up the storage for multer
const storage = multer_1.default.diskStorage({
    destination: function (req, file, cb) {
        cb(null, 'src/uploads/');
    },
    filename: function (req, file, cb) {
        const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
        cb(null, uniqueSuffix + '-' + file.originalname);
    }
});
const upload = (0, multer_1.default)({ storage: storage });
thingRouter.post('/', upload.single('image'), async (req, res, next) => {
    try {
        const newThingName = req.body.name;
        const newThingImageUrl = req.file ? req.file.filename : ''; // Use the filename generated by multer
        thingModel.createThing(newThingName, newThingImageUrl, (error, thingId) => {
            if (error) {
                return next(error);
            }
            return res.status(201).json({ thingId });
        });
    }
    catch (error) {
        // Handle any uncaught error here
        return next(error);
    }
});
thingRouter.put('/:id/removeImage', async (req, res, next) => {
    try {
        console.log(req.body);
        const thingId = Number(req.params.id);
        const thingImageUrl = req.body.image_url;
        thingModel.updateRemoveThingPhoto(thingId, thingImageUrl, (error, thingId) => {
            if (error) {
                return next(error);
            }
            return res.status(200).json({ thingId });
        });
    }
    catch (error) {
        // Handle any uncaught error here
        return next(error);
    }
});
thingRouter.get('/', async (req, res) => {
    thingModel.findAllThings((error, things) => {
        if (error) {
            return res.status(500).json({ errorMessage: error.message });
        }
        return res.status(200).json(things);
    });
});
thingRouter.get('/:id', async (req, res, next) => {
    try {
        const thingId = Number(req.params.id);
        thingModel.findOneThing(thingId, (error, thing) => {
            if (error) {
                return next(error);
            }
            return res.status(200).json(thing);
        });
    }
    catch (error) {
        // Handle any uncaught error here
        return next(error);
    }
});
thingRouter.put('/:id', async (req, res) => {
    const thing = req.body;
    thingModel.updateThing(thing, (error) => {
        if (error) {
            return res.status(500).json({ errorMessage: error.message });
        }
        return res.status(200).send();
    });
});
thingRouter.delete('/:id', async (req, res) => {
    const thingId = Number(req.params.id);
    thingModel.deleteThing(thingId, (error) => {
        if (error) {
            return res.status(500).json({ errorMessage: error.message });
        }
        return res.status(200).send();
    });
});
